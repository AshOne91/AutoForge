# Next Task

## Next executable unit: establish the generated notification boundary

Trace reusable notification responsibilities in the reference base server and
add one opt-in AutoForge runtime boundary with a generic asynchronous contract,
deterministic fake, explicit lifecycle, and no application-specific channel or
provider workflow. Keep templates, credentials, recipient policy, persistence,
retry/deduplication, and delivery observability consumer-owned.

Do not change the KIS default Redis specification, treat a Realtime boundary as
an EventBus replacement, create a live provider integration, or run the
explicitly opt-in KIS balance integration check automatically.
